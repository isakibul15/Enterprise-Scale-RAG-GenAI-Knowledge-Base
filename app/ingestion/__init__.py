# Import directly from the specific module files, not from this package.
# e.g. from app.ingestion.pipeline import IngestionPipeline
#
# Eager re-exports here create a circular import chain via vector_store.

